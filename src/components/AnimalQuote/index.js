import { Blockquote } from "rebass";
import styled from "styled-components";

const AnimalQuote = styled(Blockquote)`
  font-family: 'Montserrat', sans-serif;
  font-size: 2rem;
  max-width: 700px;
  margin-bottom: 2em;
`;

export default AnimalQuote;
