import styled from "styled-components";

const NotificationEvent = styled.h1`
  padding: 0 15px;
  font-weight: normal;
  font-size: 16px;
  letter-spacing: 1px;
`;

export default NotificationEvent;
