import { Text } from "rebass";
import styled from "styled-components";

const DreamText = styled(Text)`
  font-family: "Times New Roman";
  color: rgba(65, 143, 253, 0.7);
  font-weight: bold;
  line-height: 1.3em;
  max-width: 38em;
  margin-bottom: 1.5em;
`;

export default DreamText;
