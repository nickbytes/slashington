import styled from "styled-components";

const PetFormLabel = styled.label`
  margin: 0 0 2px;
  font-size: 16px;
  line-height: 16px;
  color: #000;
  font-weight: normal;
`;

export default PetFormLabel;
