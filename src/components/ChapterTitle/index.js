import { Heading } from "rebass";
import styled from "styled-components";

const ChapterTitle = styled(Heading)`
  font-family: "Courier Neue", courier, monospace;
  font-weight: normal;
  color: rgb(32, 32, 32);
  font-size: 3rem;
`;

export default ChapterTitle;
